# Документация проекта NO LIMITS 2.0

## 📋 Содержание

- [Обзор проекта](#обзор-проекта)
- [Архитектура](#архитектура)
- [Настройка окружения](#настройка-окружения)
- [Разработка](#разработка)
- [API документация](#api-документация)
- [База данных](#база-данных)
- [Деплой](#деплой)

---

## Обзор проекта

**NO LIMITS 2.0** — Telegram Mini App с полным стеком технологий для управления данными, аналитики и работы с GPT API.

### Основные возможности

- 📊 Визуализация данных (графики, диаграммы)
- 🤖 Интеграция с OpenAI GPT API
- 👤 Аутентификация пользователей
- 📈 Статистика и аналитика
- 🎨 Современный UI с поддержкой темной/светлой темы
- 💬 Telegram бот с командой /start
- 📱 Telegram Mini App интеграция
- 🚀 Онбординг пользователей (5 экранов)
- 🍽️ Дневник питания: блюда по дням (календарь), КБЖУ, вода
- ⭐ Избранное: сохранение блюд и быстрое добавление в рацион
- 📷 Камера AI / Фото анализ: распознавание еды (OpenAI + Open Food Facts)
- 🦶 Шаги и 🌙 сон: добавление и просмотр по дням
- 🧭 Главный экран (дашборд): прогрессы, графики, подсказки (пока заглушки)

---

## Архитектура

### Frontend

- **React 18** + **TypeScript** + **Vite**
- **Tailwind CSS** + **shadcn/ui** для UI
- **Zustand** для управления состоянием
- **React Query** для работы с API
- **React Router** для навигации
- **Framer Motion** для анимаций

### Backend

- **Node.js** + **Express** + **TypeScript**
- **PostgreSQL** + **Prisma ORM**
- **JWT** для аутентификации
- **OpenAI API** для работы с GPT
- **Telegraf** для работы с Telegram Bot API
- **Zod** для валидации данных

### Структура проекта

```
NO LIMITS. 2.0/
├── frontend/                    # React приложение
│   ├── public/                  # Статические файлы
│   ├── src/
│   │   ├── api/                 # API клиенты (Axios instances)
│   │   ├── components/          # React компоненты
│   │   │   ├── charts/          # Компоненты графиков (Recharts, Nivo)
│   │   │   ├── common/          # Общие компоненты (Loading, Error и т.д.)
│   │   │   ├── layout/          # Layout компоненты (Header, Sidebar, Footer)
│   │   │   ├── theme/           # Компоненты темы (ThemeToggle, ThemeProvider)
│   │   │   └── ui/              # shadcn/ui компоненты (Button, Card, Input и т.д.)
│   │   ├── hooks/               # Кастомные React хуки
│   │   ├── lib/                 # Утилиты и вспомогательные функции
│   │   ├── pages/               # Страницы приложения (роуты)
│   │   ├── store/               # Zustand stores (глобальное состояние)
│   │   ├── styles/              # Глобальные стили
│   │   │   └── globals.css      # Основной CSS файл
│   │   ├── types/               # TypeScript типы и интерфейсы
│   │   ├── App.tsx              # Главный компонент приложения
│   │   ├── main.tsx             # Точка входа приложения
│   │   └── vite-env.d.ts        # Типы для Vite
│   ├── index.html               # HTML шаблон
│   ├── package.json             # Зависимости и скрипты
│   ├── package-lock.json        # Зафиксированные версии зависимостей
│   ├── tsconfig.json            # TypeScript конфигурация
│   ├── tsconfig.node.json       # TypeScript конфигурация для Node.js
│   ├── vite.config.ts           # Конфигурация Vite
│   ├── tailwind.config.js       # Конфигурация Tailwind CSS
│   └── postcss.config.js        # Конфигурация PostCSS
│
├── backend/                     # Express сервер
│   ├── prisma/                  # Prisma ORM
│   │   ├── schema.prisma        # Схема базы данных
│   │   └── migrations/          # Миграции базы данных
│   ├── src/
│   │   ├── controllers/         # Контроллеры (обработка HTTP запросов)
│   │   ├── middleware/          # Express middleware (auth, validation, error handling)
│   │   ├── models/              # Prisma модели (генерируются автоматически)
│   │   ├── routes/              # API маршруты (роутинг)
│   │   ├── services/              # Бизнес-логика (сервисный слой)
│   │   ├── types/               # TypeScript типы и интерфейсы
│   │   ├── utils/               # Утилиты и вспомогательные функции
│   │   └── index.ts             # Точка входа сервера
│   ├── node_modules/            # Зависимости
│   ├── package.json             # Зависимости и скрипты
│   ├── package-lock.json        # Зафиксированные версии зависимостей
│   ├── tsconfig.json            # TypeScript конфигурация
│   └── README.md                # Документация бэкенда
│
├── docs/                        # Документация проекта
│   └── README.md                # Основная документация (этот файл)
│
├── README.md                    # Главный README проекта
└── STACK.md                     # Подробное описание стека технологий
```

#### Описание директорий

**Frontend (`frontend/`)**

- `src/api/` - API клиенты для взаимодействия с бэкендом (Axios instances, методы запросов)
- `src/components/charts/` - Компоненты для визуализации данных (Recharts, Nivo)
- `src/components/common/` - Переиспользуемые компоненты (Loading, Error, EmptyState)
- `src/components/layout/` - Компоненты макета (Header, Sidebar, Footer, Container)
- `src/components/theme/` - Компоненты для работы с темами (темная/светлая)
- `src/components/onboarding/` - Компоненты онбординга (OnboardingButton, GoalCard, ActivityCard)
- `src/components/ui/` - Базовые UI компоненты из shadcn/ui
- `src/hooks/` - Кастомные React хуки (useTelegram, useTheme, useTypewriter)
- `src/lib/` - Утилиты (cn, calculations, formatters)
- `src/pages/` - Страницы приложения (LaunchScreen, OnboardingScreen1-4)
  - `src/pages/MainScreen.tsx` - контейнер с календарем/таббаром и экранами
  - `src/pages/HomeScreen.tsx` - главный дашборд (заглушки)
  - `src/pages/NutritionScreen.tsx` - питание/вода/дневник по дням
  - `src/pages/ActivityScreen.tsx` - шаги/сон по дням
  - `src/pages/FavoritesScreen.tsx` - избранное
  - `src/pages/ChatScreen.tsx` - чат AI
  - `src/pages/FoodAnalysisView.tsx` - результаты анализа еды по фото
- `src/store/` - Zustand stores для глобального состояния
- `src/types/` - TypeScript типы и интерфейсы (onboarding, user)

**Backend (`backend/`)**

- `src/controllers/` - Контроллеры обрабатывают HTTP запросы и возвращают ответы
- `src/middleware/` - Middleware для аутентификации, валидации, обработки ошибок
- `src/models/` - Prisma модели (генерируются автоматически из schema.prisma)
- `src/routes/` - Определение API маршрутов и их привязка к контроллерам
- `src/services/` - Бизнес-логика приложения (работа с БД, внешними API)
- `src/types/` - TypeScript типы и интерфейсы
- `src/utils/` - Утилиты (logger, errors, helpers)
- `prisma/` - Схема базы данных и миграции

---

## Настройка окружения

### Требования

- Node.js >= 18.x
- PostgreSQL >= 14.x
- npm или yarn

### Frontend

1. Установка зависимостей:
```bash
cd frontend
npm install
```

2. Создайте `.env.local`:
```env
VITE_API_URL=http://localhost:5001
VITE_BOT_TOKEN=your_bot_token
```

Для работы с Cloudpub:
```env
VITE_API_URL=https://murkily-cosmic-hummingbird.cloudpub.ru
VITE_BOT_TOKEN=your_bot_token
```

3. Запуск dev сервера:
```bash
npm run dev
```

### Backend

1. Установка зависимостей:
```bash
cd backend
npm install
```

2. Создайте `.env`:
```env
PORT=5001
NODE_ENV=development
CORS_ORIGIN=http://localhost:3000

# База данных (Supabase Postgres) — обязательна
# Возьми строку подключения в Supabase → Settings → Database → Connection string
# Рекомендуемый вариант: Session pooler (IPv4 compatible)
# DATABASE_URL="postgresql://postgres.<project_ref>:<password>@<region>.pooler.supabase.com:5432/postgres?sslmode=require&pgbouncer=true"

OPENAI_API_KEY=your_openai_api_key
JWT_SECRET=your_jwt_secret
JWT_EXPIRES_IN=7d

BOT_TOKEN=your_telegram_bot_token

# Cloudpub (опционально)
CLOUDPUB_BACKEND_URL=https://murkily-cosmic-hummingbird.cloudpub.ru
CLOUDPUB_FRONTEND_URL=https://disgustingly-benign-cow.cloudpub.ru
```

3. Настройка базы данных (опционально):
```bash
# Только если настроили DATABASE_URL
# Генерация Prisma клиента
npm run prisma:generate

# Применение миграций
npm run prisma:migrate
```

**Примечание:** База данных не обязательна. Приложение работает без неё. Подробнее в [DATABASE.md](./DATABASE.md).

4. Запуск сервера:
```bash
npm run dev
```

---

## Разработка

### Соглашения о коде

- **TypeScript**: Строгая типизация, избегаем `any`
- **SOLID принципы**: Следование принципам SOLID
- **KISS**: Простота и читаемость кода
- **Компоненты**: Функциональные компоненты с хуками
- **Именование**: camelCase для переменных, PascalCase для компонентов

### Онбординг

Приложение включает 4 экрана онбординга:

1. **LaunchScreen** (`/`) - Экран запуска с анимацией печатания "NO LIMITS."
2. **OnboardingScreen1** (`/onboarding/1`) - Приветственный экран с текстом и кнопкой "Начать движение →"
3. **OnboardingScreen2** (`/onboarding/2`) - Выбор цели (Лёгкость, Баланс, Сила)
4. **OnboardingScreen3** (`/onboarding/3`) - Выбор уровня активности (Редко, Иногда, Часто, Постоянно, Интенсивно)
5. **OnboardingScreen4** (`/onboarding/4`) - Ввод данных профиля (пол, рост, вес, возраст)

**Особенности:**
- Данные сохраняются в базу данных при переходе между экранами
- Выбранные значения предзагружаются при повторном открытии экрана
- Используются иконки из `lucide-react`
- Glass material эффекты для всех карточек и полей ввода
- Поддержка темной и светлой темы
- Адаптивный дизайн для мобильных устройств
- Поля ввода с фокусом и анимацией
- Segmented control для выбора пола

### Структура компонентов

```typescript
// Пример структуры компонента
import { useState } from 'react';
import { useQuery } from '@tanstack/react-query';

interface ComponentProps {
  // типы пропсов
}

export const Component = ({ ...props }: ComponentProps) => {
  // логика компонента
  return (
    // JSX
  );
};
```

### Работа с API

```typescript
// Пример использования React Query
const { data, isLoading, error } = useQuery({
  queryKey: ['users'],
  queryFn: () => api.getUsers(),
});
```

---

## API документация

### Nutrition

- `GET /api/nutrition/stats?date=YYYY-MM-DD&telegramUserId=...` — дневная статистика (КБЖУ + группы приемов пищи)
- `POST /api/nutrition/food` — сохранить блюдо (с датой)
- `DELETE /api/nutrition/food/:id` — удалить блюдо
- `GET /api/nutrition/water?date=YYYY-MM-DD&telegramUserId=...` — вода за день
- `POST /api/nutrition/water` — добавить воду
- `POST /api/nutrition/water/decrement` — удалить последнюю запись воды за день

### Favorites

- `GET /api/favorites?telegramUserId=...` — список избранного
- `POST /api/favorites/toggle` — toggle избранного (создать/удалить по foodName)
- `DELETE /api/favorites/:id?telegramUserId=...` — удалить из избранного

### Activity (steps/sleep)

- `GET /api/activity/steps?date=YYYY-MM-DD&telegramUserId=...` — шаги за день (сумма + записи)
- `POST /api/activity/steps` — добавить шаги
- `POST /api/activity/steps/decrement` — удалить последнюю запись шагов за день
- `GET /api/activity/steps/month?year=YYYY&month=MM&telegramUserId=...` — агрегация шагов по месяцу для календаря
- `GET /api/activity/sleep?date=YYYY-MM-DD&telegramUserId=...` — сон за день
- `POST /api/activity/sleep` — добавить сон


### Базовый URL

**Локально:**
```
http://localhost:5001/api
```

**Cloudpub:**
```
https://murkily-cosmic-hummingbird.cloudpub.ru/api
```

### Аутентификация

Большинство эндпоинтов требуют JWT токен в заголовке:

```
Authorization: Bearer <token>
```

### Основные эндпоинты

#### Аутентификация

- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `POST /api/auth/refresh` - Обновление токена

#### Пользователи

- `POST /api/users/check` - Проверка/создание пользователя по данным Telegram
  ```json
  {
    "id": 123456789,
    "first_name": "Иван",
    "last_name": "Иванов",
    "username": "ivanov"
  }
  ```
  Ответ:
  ```json
  {
    "success": true,
    "user": {
      "id": 1,
      "telegramId": "123456789",
      "onboarding": {
        "isCompleted": false,
        "goal": "lose_weight",
        "activityLevel": "often"
      }
    }
  }
  ```

- `POST /api/users/goal` - Сохранение/обновление цели пользователя
  ```json
  {
    "userId": 1,
    "goal": "lose_weight"
  }
  ```
  Возможные значения: `lose_weight`, `maintain`, `gain_muscle`

- `POST /api/users/activity-level` - Сохранение/обновление уровня активности
  ```json
  {
    "userId": 1,
    "activityLevel": "often"
  }
  ```
  Возможные значения: `rarely`, `sometimes`, `often`, `constant`, `intensive`

- `POST /api/users/profile` - Сохранение/обновление профиля пользователя
  ```json
  {
    "userId": 1,
    "gender": "male",
    "height": 180,
    "weight": 75.5,
    "age": 30
  }
  ```
  Все поля опциональны. Возможные значения `gender`: `male`, `female`

#### GPT

- `POST /api/gpt/chat` - Отправить запрос к GPT
  ```json
  {
    "message": "Ваш вопрос",
    "systemPrompt": "Опциональный системный промпт"
  }
  ```

#### Telegram Bot

Бот автоматически запускается при старте сервера и обрабатывает команды:
- `/start` - Приветственное сообщение с кнопкой для открытия Mini App

---

## База данных

База данных настроена и используется для хранения данных пользователей и онбординга.

### Структура

**Таблица `users`:**
- `id` (Int, автоинкремент) - уникальный идентификатор
- `telegramId` (String, уникальный) - ID пользователя в Telegram
- `createdAt`, `updatedAt` - временные метки

**Таблица `onboarding_data`:**
- `id` (UUID) - уникальный идентификатор
- `userId` (Int, FK, уникальный) - ссылка на пользователя (один к одному)
- `goal` (String, опционально) - цель пользователя: `lose_weight`, `maintain`, `gain_muscle`
- `activityLevel` (String, опционально) - уровень активности: `rarely`, `sometimes`, `often`, `constant`, `intensive`
- `gender` (String, опционально) - пол пользователя: `male`, `female`
- `height` (Int, опционально) - рост в см
- `weight` (Float, опционально) - вес в кг
- `age` (Int, опционально) - возраст
- `isCompleted` (Boolean) - флаг завершения онбординга (по умолчанию `false`)
- `createdAt`, `updatedAt` - временные метки

**Принцип:** Поля добавляются в БД только по мере необходимости. Не создаем лишние колонки заранее.

### Настройка базы данных

Подробная инструкция по настройке PostgreSQL (Supabase, Yandex Cloud) в [DATABASE.md](./DATABASE.md).

### Prisma

#### Prisma Schema

Схема описана в `backend/prisma/schema.prisma`. Текущая структура минималистична.

#### Миграции

```bash
# Создать новую миграцию (при добавлении полей)
cd backend
npx prisma migrate dev --name add_field_name

# Применить миграции
npx prisma migrate deploy

# Генерация Prisma Client
npx prisma generate
```

#### Prisma Studio

Визуальный редактор базы данных:

```bash
cd backend
npx prisma studio
```

Откроется браузер на `http://localhost:5555`.

---

## Деплой

### Frontend

1. Сборка проекта:
```bash
cd frontend
npm run build
```

2. Деплой статических файлов из `dist/` на хостинг (Vercel, Netlify и т.д.)

### Backend

1. Сборка проекта:
```bash
cd backend
npm run build
```

2. Запуск в продакшене:
```bash
npm start
```

### Переменные окружения

Убедитесь, что все переменные окружения настроены на продакшн сервере.

---

## Полезные ссылки

- [STACK.md](../STACK.md) - Полный список технологий
- [README.md](../README.md) - Быстрый старт
- [Backend README](../backend/README.md) - Документация бэкенда
- [CLOUDPUB.md](./CLOUDPUB.md) - Настройка Cloudpub туннелей
- [TELEGRAM_SETUP.md](./TELEGRAM_SETUP.md) - Настройка Telegram бота
- [LOGGING.md](./LOGGING.md) - Система логирования
- [DATABASE.md](./DATABASE.md) - Настройка базы данных (опционально)

---

## Контакты и поддержка

Для вопросов и предложений создавайте issues в репозитории проекта.

